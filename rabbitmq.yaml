- name: setup rabbitmq server
  hosts: rabbitmq
  become: yes

# create rabbitmq tasks

  tasks:

  - name: copy rabbitmq.repo file 
    ansible.builtin.copy:
      src: rabbitmq.repo
      dest: /etc/yum.repos.d/rabbitmq.repo
